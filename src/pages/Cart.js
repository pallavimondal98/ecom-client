import React from 'react'
import CartItems from '../Components/CartItems/CartItems'

const Cart = () => {
    return (
        // <!-- Cart Section -->
        <CartItems/>
    )
}

export default Cart